This circuit consists basically in ```motor_analogpos_test```on top of which a small resistance is used for measuring intensity, i.e. servo's torque proxy.

For beginner's sake, scheme is given with plain wiring.